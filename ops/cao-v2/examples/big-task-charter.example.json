{
  "schema_version": "2.0",
  "run_id": "example-run-001",
  "big_task_id": "TASK-A",
  "outcome": "Deliver the complete observable outcome for TASK-A without weakening frozen acceptance.",
  "acceptance": [
    "The user-visible behavior matches the milestone acceptance slice.",
    "Required tests and independent review pass.",
    "The exact accepted commit is integrated and recorded."
  ],
  "in_scope": ["packages/example/src", "packages/example/tests"],
  "out_of_scope": ["unrelated refactors", "production deployment"],
  "constraints": ["Preserve public interfaces unless the supervisor approves an exception."],
  "risk_class": "MEDIUM",
  "delegation_envelope": {
    "allowed_routes": [
      "agy_flash_high",
      "agy_pro_high",
      "claude_subscription_sonnet",
      "codex_plus_sol"
    ],
    "maximum_tier": 4,
    "gateway_allowed": false,
    "protected_supervisor_pool_use_allowed": false,
    "max_phases": 4,
    "max_parallel_writers": 2,
    "supervisor_approval_triggers": [
      "acceptance_change",
      "material_scope_change",
      "architecture_outside_charter",
      "gateway_use",
      "protected_reserve_use",
      "capability_above_tier_4",
      "material_budget_expansion"
    ]
  },
  "review_policy": {
    "default_class": "routine_independent",
    "critical_requires_cross_provider": true
  },
  "completion_boundary": "The Task Lead may recommend big-task completion; only the authoritative supervisor may finalize the milestone."
}
