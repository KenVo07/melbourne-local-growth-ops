# Phase TASK-A-P01

Implement only the bounded objective in the Phase Packet.

## Verified context

- The shared interface is already approved and must not change.
- The phase owns only the paths listed in the packet.
- Do not stage or commit; the host performs Git metadata operations.

## Required behavior

1. Implement the requested consumer using the existing pattern.
2. Add or update only tests needed for this behavior.
3. Run the targeted checks from the packet.
4. Return the exact structured result envelope supplied by the host prompt.
