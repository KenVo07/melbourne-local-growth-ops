You are the implementation and empirical-acceptance owner for MLGO CAO v2 SLICE 4.

Repository:
`/home/khoa/Projects/melbourne-local-growth-ops`

Canonical starting point at handoff:
`origin/main@d6f90ef9babdc7ba641499bec2c02509e623a2b5`

Read first:
1. `MLGO_CAO_V2_SLICE4_EMPIRICAL_ACCEPTANCE_CONTRACT.md`
2. `MLGO_CAO_V2_SLICE4_HIGH_SIGNAL_MATRIX.md`
3. root `AGENTS.md`
4. accepted ADRs
5. current `ops/cao-v2` source
6. Slice 3.5 merged PR/evidence if needed

Do not ask the operator to perform routine Git, worktree, test, staging, restart, evidence or PR steps.

## First action

- fetch origin/main;
- confirm whether main still equals the handoff SHA;
- if main moved, inspect and reconcile rather than resetting blindly;
- create an isolated Slice 4 branch/worktree from current origin/main.

Preferred branch:
`integration/cao-v2-slice4-20260810-local-1`

## Mission

Deliver ONE coherent Slice 4 vertical slice:

LIVE CANARY INTEGRATION + EMPIRICAL ACCEPTANCE.

This is not another architecture review and not another synthetic-test expansion.

Wire the already-built mechanisms into a canary-safe real dispatch path, then prove them using a
small number of high-signal real-host scenarios.

## Required integration work

Before empirical provider runs, close the remaining integration seams cleanly:

1. ContextEnvelope must serialize through the actual provider-send path.
2. Budget reservation/settlement must wrap the actual provider-send path.
3. SkillContract/native mirror selection must be applied before provider dispatch.
4. ApprovalBroker + PermissionAdapter must govern the relevant permission/tool boundary.
5. Event/checkpoint/authority state must be durable around the real call.
6. provider-specific permission behavior must remain behind adapter/capability boundaries.

Do not special-case provider brand names in controller semantics.

Provider-specific mechanics belong in adapters/registry data.

## Controller lifecycle

Resolve the boot-autostart residual structurally.

Do NOT implement "keep stopping the controller after reboot" as the design.

Target:
- the service may be enabled and may start automatically;
- process liveness grants no execution authority;
- in global `v2_shadow`, controller activity is observation/reconciliation only;
- real canary execution requires an explicit disposable canary scope;
- after canary, safe shadow posture is restored automatically.

Prove a start/restart in `v2_shadow` cannot emit a real provider call.

If a new canary mode or equivalent scope mechanism is needed, keep it narrow and provider-neutral.

## Native canonical skills

Reconcile the already-approved canonical skills from the sealed cache into the native mirror.

Authorized bundles only:
- Addy Osmani exact locked revision;
- Matt Pocock exact locked revision;
- UI/UX Pro Max exact locked revision;
- UnitOneAI SecuritySkills exact locked revision.

Do not fetch a different revision.
Do not add Mukul.
Do not run upstream setup scripts.
Do not use a generic skill marketplace.

The mirror is execution plumbing; sealed cache remains authoritative.

## Real provider qualification

Inspect the actual installed provider/harness permission mechanisms on this host.

Prefer native configuration/preauthorization over UI automation.

Use only already-authorized subscription-backed routes.
No PAYG fallback.
No Business 2.

Target:
- at least 2 real provider execution paths QUALIFIED if available;
- 3 concurrent project runs total;
- at most 12 real provider sessions;
- prefer 6-9.

If only one safe provider path is available, still complete all possible work, mark the second-provider
criterion PARTIAL, and explain the exact blocker. Do not invent a workaround or buy access.

Never use "Approve and remember" as CAO authority.

Never build a blind auto-clicker.

## Three-project canary

Create three disposable isolated projects/worktrees.

Deliberately reuse human-friendly task/Command names across them.

Recommended shapes:
- Project A: small frontend task, UIUX + Matt recipe.
- Project B: small backend/debug task, Addy + Matt recipe.
- Project C: security-sensitive disposable fixture, UnitOneAI recipe.

Keep all work disposable and outside production/client deployment paths.

Run them concurrently.

## Permission scenarios

Exercise:
- safe read;
- safe delegated write;
- out-of-scope write denial;
- cross-security-domain denial;
- semantic conflict escalation;
- operator-only credential/production/destructive requests as NON-EXECUTED fixtures.

Do not execute destructive/operator-only actions merely to prove denial.

## Crash/recovery

During active canary work, perform a controlled controller/worker interruption at a known checkpoint.

Prove:
- restart reconstructs durable state;
- no duplicate provider/tool action;
- no double budget settlement;
- no stale approval applied;
- unaffected projects continue or recover independently.

No full host reboot is required unless separately authorized. A service/controller restart is enough
for this Slice 4 acceptance if it exercises the same durable lifecycle boundary.

## Security canary

Build a local disposable fixture with 5-7 seeded application-security flaws representative of an
AI-assisted commodity attacker.

Use UnitOneAI-backed review.

No third-party target.
No internet attack.
No real credential exposure.

Require >=80% material seeded-flaw detection with evidence.

## Cost/quality

Record provider-native usage.

Do not fabricate dollars when the route is subscription/quota based.

Track:
- provider sessions;
- input/output/native units;
- retries;
- skill context bytes/tokens;
- wall time;
- task result quality.

Prefer minimal skill recipes.

Ordinary tasks should target <=3 external skills.

Do not lower quality just to reduce token count.

## 100-session soak clarification

Do NOT run 100 paid/model sessions.

Run >=100 local zero-provider-call approval/skill/restart/collision cases.

Real provider sessions remain capped at 12.

## Bounded self-stabilization

You own routine repair loops.

If empirical runs expose a bounded implementation defect:
- fix it on the same Slice 4 branch;
- add one focused regression;
- rerun the affected empirical scenario and relevant existing verification;
- continue.

Do not ask the operator for ordinary code/debug/test decisions.

Stop only for the operator-only conditions in the contract.

Maximum: 3 material code-fix cycles before stop/report.

## PR and evidence

Create a DRAFT PR only after the empirical run is complete or a material blocker is reached.

Suggested title:
`feat(cao): complete live canary and empirical acceptance`

Do not merge it.

Evidence directory must contain at least:

1. `MLGO_CAO_V2_SLICE4_IMPLEMENTATION_AND_CANARY_REPORT.md`
2. `MLGO_CAO_V2_SLICE4_ACCEPTANCE_EVIDENCE_INDEX.json`
3. `MLGO_CAO_V2_SLICE4_THREE_PROJECT_ISOLATION_REPORT.md`
4. `MLGO_CAO_V2_SLICE4_PERMISSION_ADAPTER_REPORT.md`
5. `MLGO_CAO_V2_SLICE4_RESTART_RECOVERY_REPORT.md`
6. `MLGO_CAO_V2_SLICE4_NATIVE_SKILL_REPORT.md`
7. `MLGO_CAO_V2_SLICE4_SECURITY_CANARY_REPORT.md`
8. `MLGO_CAO_V2_SLICE4_COST_QUALITY_REPORT.md`
9. `MLGO_CAO_V2_SLICE4_FINAL_HANDOFF.md`

Preserve raw logs/records needed to substantiate every empirical claim.

## Absolute boundaries

- no production deployment;
- no client/customer mutation;
- no production enforcement;
- no PAYG;
- no Business 2;
- no new credentials;
- no force push;
- no broad destructive rollback/cleanup;
- no new external skill/vendor;
- no Mukul;
- no Web Reasoning Bridge;
- no merge;
- no 100 paid provider sessions.

## Final verdict

End the main report exactly:

SLICE4_EMPIRICAL_ACCEPTANCE: PASS/FAIL/PARTIAL
LIVE_CONTEXT_ENVELOPE_DISPATCH: PASS/FAIL
LIVE_BUDGET_RESERVATION_SETTLEMENT: PASS/FAIL
LIVE_SKILL_CONTRACT_DISPATCH: PASS/FAIL
LIVE_APPROVAL_BROKER: PASS/FAIL
PERMISSION_ADAPTER_QUALIFIED_PATHS: <integer>
THREE_PROJECT_CONCURRENCY: PASS/FAIL
CROSS_PROJECT_LEAKAGE: 0/<nonzero>
RESTART_RECOVERY: PASS/FAIL
DUPLICATE_PROVIDER_OR_TOOL_ACTIONS: 0/<nonzero>
NATIVE_MIRROR_READY: YES/NO
UNITONEAI_SECURITY_CANARY: PASS/FAIL
SEEDED_SECURITY_FINDINGS_CAUGHT: <integer>/<integer>
UNEXPECTED_INTERACTIVE_APPROVAL_STALLS: 0/<nonzero>
UNAUTHORIZED_SILENT_APPROVALS: 0/<nonzero>
ZERO_PROVIDER_SOAK_CASES: <integer>
REAL_PROVIDER_SESSIONS: <integer>
PAYG_USED: NO
BUSINESS2_USED: NO
PRODUCTION_ENFORCEMENT_CHANGED: NO
CONTROLLER_BOOT_AUTOSTART_SAFE_POSTURE: CLOSED/OPEN
CONTEXT_ENVELOPE_DISPATCH_RESIDUAL: CLOSED/OPEN
BUDGET_DISPATCH_WIRING_RESIDUAL: CLOSED/OPEN
SAFE_SHADOW_POSTURE_RESTORED: YES/NO
WEB_BRIDGE_STARTED: NO
READY_FOR_SLICE4_REVIEW: YES/NO
READY_FOR_WEB_REASONING_BRIDGE: YES/NO

STOP after draft PR + evidence.
